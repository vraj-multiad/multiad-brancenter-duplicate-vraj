<%= form_for(@opt_out) do |f| %>
  <% if @opt_out.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opt_out.errors.count, "error") %> prohibited this opt_out from being saved:</h2>

      <ul>
      <% @opt_out.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email_address %><br>
    <%= f.text_area :email_address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
