<%= form_for(@dl_cart) do |f| %>
  <% if @dl_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dl_cart.errors.count, "error") %> prohibited this dl_cart from being saved:</h2>

      <ul>
      <% @dl_cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
