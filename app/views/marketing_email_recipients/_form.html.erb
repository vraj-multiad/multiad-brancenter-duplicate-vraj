<%= form_for(@marketing_email_recipient) do |f| %>
  <% if @marketing_email_recipient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marketing_email_recipient.errors.count, "error") %> prohibited this marketing_email_recipient from being saved:</h2>

      <ul>
      <% @marketing_email_recipient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :marketing_email_id %><br>
    <%= f.number_field :marketing_email_id %>
  </div>
  <div class="field">
    <%= f.label :email_address %><br>
    <%= f.text_field :email_address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
