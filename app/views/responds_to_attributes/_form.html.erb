<%= form_for(@responds_to_attribute) do |f| %>
  <% if @responds_to_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@responds_to_attribute.errors.count, "error") %> prohibited this responds_to_attribute from being saved:</h2>

      <ul>
      <% @responds_to_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :respondable_id %><br>
    <%= f.number_field :respondable_id %>
  </div>
  <div class="field">
    <%= f.label :respondable_type %><br>
    <%= f.text_field :respondable_type %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
